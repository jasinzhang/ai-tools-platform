# 🔧 推送问题排查和解决方案

## 常见推送问题

### 问题1: 认证失败

**症状**：提示用户名或密码错误

**解决方案**：
1. 确保使用Personal Access Token，不是GitHub密码
2. Token需要有`repo`权限
3. 如果Token过期，重新生成

### 问题2: 连接超时

**症状**：推送卡住，没有响应

**解决方案**：
1. 检查网络连接
2. 可能需要使用代理
3. 尝试使用SSH方式

### 问题3: 仓库地址错误

**症状**：提示找不到仓库

**解决方案**：
```bash
# 检查远程地址
git remote -v

# 如果错误，重新设置
git remote set-url origin https://github.com/jasinzhang/ai-tools-platform.git
```

### 问题4: 分支名称不匹配

**症状**：提示分支不存在

**解决方案**：
```bash
# 确保分支是main
git branch -M main

# 然后推送
git push -u origin main
```

---

## 🛠️ 快速修复命令

### 重置远程连接

```bash
# 删除现有远程
git remote remove origin

# 重新添加
git remote add origin https://github.com/jasinzhang/ai-tools-platform.git

# 验证
git remote -v
```

### 使用SSH方式（如果HTTPS有问题）

```bash
# 更改远程地址为SSH
git remote set-url origin git@github.com:jasinzhang/ai-tools-platform.git

# 推送
git push -u origin main
```

### 强制推送（谨慎使用）

```bash
# 只在确定要覆盖远程代码时使用
git push -u origin main --force
```

---

## 🔍 诊断步骤

1. **检查Git状态**
   ```bash
   git status
   ```

2. **检查远程配置**
   ```bash
   git remote -v
   ```

3. **检查分支**
   ```bash
   git branch -a
   ```

4. **测试连接**
   ```bash
   git ls-remote origin
   ```

---

## 💡 推荐解决方案

### 方案1: 使用GitHub CLI（最简单）

```bash
# 安装GitHub CLI
brew install gh

# 登录
gh auth login

# 推送
git push -u origin main
```

### 方案2: 使用SSH密钥

如果你已经配置了SSH密钥：

```bash
git remote set-url origin git@github.com:jasinzhang/ai-tools-platform.git
git push -u origin main
```

### 方案3: 重新生成Token

1. 访问：https://github.com/settings/tokens
2. 删除旧的token
3. 生成新的token（确保有repo权限）
4. 重新推送

---

**告诉我具体的错误信息，我可以提供更精确的解决方案！**
