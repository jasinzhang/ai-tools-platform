# 🚀 推送到GitHub - 认证说明

## ✅ 已完成

- ✅ 远程仓库已连接：https://github.com/jasinzhang/ai-tools-platform.git
- ✅ 代码已准备好推送

## 🔐 需要GitHub认证

推送时需要输入GitHub凭据。有两种方式：

---

## 方式1: 使用Personal Access Token（推荐）

### 步骤1: 获取Token

1. 访问：https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. 填写信息：
   - **Note**: `ai-tools-platform`
   - **Expiration**: 选择过期时间（建议90天或No expiration）
   - **Select scopes**: 勾选 **`repo`**（完整仓库权限）
4. 点击 **"Generate token"**
5. **复制token**（只显示一次！类似：`ghp_xxxxxxxxxxxxxxxxxxxx`）

### 步骤2: 推送代码

在终端运行：

```bash
git push -u origin main
```

当提示输入时：
- **Username**: `jasinzhang`
- **Password**: 粘贴刚才复制的Personal Access Token（不是GitHub密码）

---

## 方式2: 使用GitHub CLI（更简单）

### 安装GitHub CLI（如果还没有）

```bash
# macOS
brew install gh

# 或下载：https://cli.github.com
```

### 登录并推送

```bash
# 登录GitHub
gh auth login

# 然后推送
git push -u origin main
```

---

## 方式3: 使用SSH（如果已配置SSH密钥）

### 更改远程地址为SSH

```bash
git remote set-url origin git@github.com:jasinzhang/ai-tools-platform.git
git push -u origin main
```

---

## ✅ 推送成功后

访问你的仓库：
**https://github.com/jasinzhang/ai-tools-platform**

你应该能看到所有代码文件！

---

## 🎯 下一步：部署到Vercel

代码推送成功后：

1. 访问：https://vercel.com
2. 使用GitHub登录
3. 点击 **"New Project"**
4. 选择 **`jasinzhang/ai-tools-platform`** 仓库
5. 配置环境变量：
   - `GOOGLE_API_KEY` = `AIzaSyCbb54UXn-upfvq33UlavlZO18u1LhMxSM`
   - `AI_PROVIDER` = `google`
   - `NODE_ENV` = `production`
6. 点击 **"Deploy"**

几分钟后，你的网站就上线了！🎉

---

**推荐使用方式1（Personal Access Token），最简单！**
