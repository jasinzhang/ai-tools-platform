# Server Configuration
PORT=3000
NODE_ENV=development

# AI Provider Configuration
# Choose one: openai, google, or both
AI_PROVIDER=google

# Google Gemini API (Recommended - Free tier available)
GOOGLE_API_KEY=your_google_api_key_here
# Gemini Model (optional): gemini-1.5-flash-latest, gemini-1.5-pro-latest, gemini-pro
# If not specified, will try multiple models automatically
GEMINI_MODEL=gemini-1.5-flash-latest

# OpenAI API (Optional)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Google AdSense Configuration (Add after approval)
GOOGLE_ADSENSE_CLIENT_ID=ca-pub-xxxxxxxxxxxxxxxx

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=30

# Proxy Configuration (Optional - for accessing Google API in restricted networks)
# If you need to use a proxy, uncomment and configure one of the following:
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# Or with authentication:
# HTTPS_PROXY=http://username:password@proxy.example.com:8080