# 🔧 Cursor IDE 地区限制完整解决方案

## ❌ 问题说明

错误信息：
```
This model provider doesn't serve your region. 
Visit https://docs.cursor.com/account/regions for more information.
```

这是 Cursor IDE 内置 AI 功能的地区限制，不是你的代码问题。

---

## ✅ 解决方案（按推荐顺序）

### 方案 1: 在 Cursor 配置文件中设置代理（最有效）⭐⭐⭐

这是**最直接有效**的方法，直接在 Cursor 的配置文件中设置代理。

#### 步骤 1: 打开 Cursor 设置文件

1. 打开 Cursor
2. 按 `Cmd + Shift + P` (macOS) 或 `Ctrl + Shift + P` (Windows)
3. 输入 `settings json`
4. 选择 `Preferences: Open User Settings (JSON)`

#### 步骤 2: 添加代理配置

在打开的 JSON 配置文件中，添加以下内容：

```json
{
  "http.proxy": "http://127.0.0.1:7897",
  "http.proxySupport": "override",
  "cursor.general.disableHttp2": true
}
```

**重要**：
- `7897` 是你的 Clash HTTP 代理端口
- 如果端口不同，请修改为你的实际端口

#### 步骤 3: 保存并重启

1. 保存配置文件（`Cmd + S` 或 `Ctrl + S`）
2. **完全退出 Cursor**（Command+Q 或 Alt+F4）
3. 重新启动 Cursor
4. 测试 AI 功能

#### 步骤 4: 验证

1. 在 Cursor 中尝试使用 AI 功能
2. 如果不再出现地区限制错误，说明配置成功！

---

### 方案 2: 配置系统代理（备用方案）⭐

Cursor IDE 可能会使用系统代理设置。

#### 步骤 1: 确保 Clash 正在运行

你的 Clash 代理应该已经在运行（端口 7897）。

#### 步骤 2: 配置系统代理

**macOS**:
1. 打开 "系统设置" → "网络"
2. 选择当前网络连接（Wi-Fi 或以太网）
3. 点击 "详细信息" → "代理"
4. 勾选 "网页代理 (HTTP)" 和 "安全网页代理 (HTTPS)"
5. 填写：
   - 服务器：`127.0.0.1`
   - 端口：`7897`
6. 点击 "好" 保存

**Windows**:
1. 打开 "设置" → "网络和 Internet" → "代理"
2. 在 "手动代理设置" 中：
   - 打开 "使用代理服务器"
   - 地址：`127.0.0.1`
   - 端口：`7897`
3. 保存设置

#### 步骤 3: 重启 Cursor

1. 完全退出 Cursor（Command+Q 或 Alt+F4）
2. 重新启动 Cursor
3. 检查是否可以使用 AI 功能

---

### 方案 2: 配置 Cursor 使用代理（如果支持）

#### 检查 Cursor 设置

1. 打开 Cursor
2. 进入设置：`Cmd+,` (macOS) 或 `Ctrl+,` (Windows)
3. 搜索 "proxy" 或 "网络"
4. 如果有代理设置，配置为：
   - HTTP Proxy: `http://127.0.0.1:7897`
   - HTTPS Proxy: `http://127.0.0.1:7897`

**注意**：Cursor 可能不直接支持代理配置，这取决于版本。

---

### 方案 3: 使用环境变量（macOS/Linux）

在启动 Cursor 时设置代理环境变量。

#### 创建启动脚本

**macOS**:
```bash
# 创建启动脚本
cat > ~/cursor_with_proxy.sh << 'EOF'
#!/bin/bash
export HTTP_PROXY=http://127.0.0.1:7897
export HTTPS_PROXY=http://127.0.0.1:7897
export http_proxy=http://127.0.0.1:7897
export https_proxy=http://127.0.0.1:7897

# 启动 Cursor
open -a Cursor
EOF

chmod +x ~/cursor_with_proxy.sh
```

**使用方式**:
```bash
~/cursor_with_proxy.sh
```

**或者直接在终端启动**:
```bash
export HTTP_PROXY=http://127.0.0.1:7897
export HTTPS_PROXY=http://127.0.0.1:7897
open -a Cursor
```

---

### 方案 4: 使用 VPN（全局方案）

如果代理方案不行，可以使用 VPN：

1. **连接 VPN**
   - 使用全局 VPN（不是仅浏览器代理）
   - 确保所有网络流量都通过 VPN

2. **重启 Cursor**
   - 完全退出并重新启动

3. **验证**
   - 检查 Cursor AI 功能是否可用

---

### 方案 5: 切换 Cursor 的 AI 模型（如果支持）

某些 Cursor 版本可能支持切换不同的 AI 模型：

1. 打开 Cursor 设置
2. 查找 "AI Model" 或 "Model Provider" 设置
3. 尝试切换到其他可用的模型
4. 保存并重启

---

### 方案 6: 使用 Cursor 的本地模型（如果有）

某些 Cursor 版本可能支持本地运行的模型：

1. 检查 Cursor 设置中是否有 "Local Model" 选项
2. 如果有，下载并配置本地模型
3. 使用本地模型不受地区限制

---

## 🔍 诊断步骤

### 1. 检查代理是否工作

在终端运行：
```bash
curl -x http://127.0.0.1:7897 https://www.google.com
```

如果能返回内容，说明代理可用。

### 2. 检查 Cursor 的网络连接

1. 打开 Cursor
2. 查看开发者工具（如果有）
3. 检查网络请求是否通过代理

### 3. 查看 Cursor 日志

**macOS**:
```bash
# 查看 Cursor 日志
tail -f ~/Library/Logs/Cursor/*.log
```

**Windows**:
```
%APPDATA%\Cursor\logs\
```

---

## 💡 最佳实践

### 1. 保持代理常开

- 确保 Clash 设置为开机自启动
- 保持代理服务运行

### 2. 使用系统代理

- 配置系统代理（方案 1）
- 这样所有应用都可以使用代理

### 3. 备用方案

- 如果 Cursor AI 不可用，使用项目中的 Gemini API
- 通过浏览器访问 http://localhost:3000 使用 AI 工具

---

## 🎯 推荐配置流程

### 步骤 1: 配置系统代理（一次性设置）

1. 打开系统设置
2. 配置网络代理为 `127.0.0.1:7897`
3. 保存设置

### 步骤 2: 确保 Clash 开机自启

1. 打开 Clash 设置
2. 启用 "开机自启动"
3. 确保 HTTP 代理端口为 7897

### 步骤 3: 重启 Cursor

1. 完全退出 Cursor
2. 重新启动
3. 测试 AI 功能

### 步骤 4: 验证

1. 在 Cursor 中尝试使用 AI 功能
2. 如果仍然不行，使用项目中的 Gemini API 作为备用

---

## ⚠️ 注意事项

### 1. 代理可能不总是有效

- Cursor 可能使用硬编码的 API 端点
- 某些版本可能不支持代理
- 需要测试确认

### 2. 性能影响

- 使用代理可能稍微降低响应速度
- 但通常影响很小

### 3. 备用方案

- 即使 Cursor AI 不可用，项目中的 Gemini API 仍然可用
- 可以通过浏览器使用所有 AI 功能

---

## 📋 快速检查清单

- [ ] Clash 正在运行（端口 7897）
- [ ] 系统代理已配置（127.0.0.1:7897）
- [ ] Cursor 已完全重启
- [ ] 测试 Cursor AI 功能
- [ ] 如果不行，使用项目中的 Gemini API 作为备用

---

## 🔄 如果所有方案都不行

### 使用项目中的 Gemini API（推荐）

你的项目已经配置好了 Gemini API，完全不受 Cursor 限制影响：

1. **启动服务器**
   ```bash
   npm start
   ```

2. **使用 AI 工具**
   - 访问 http://localhost:3000
   - 使用所有 AI 功能
   - 通过代理访问，完全可用

3. **优势**
   - ✅ 不受 Cursor 限制影响
   - ✅ 已配置代理
   - ✅ 功能完整
   - ✅ 体验更好（通过浏览器界面）

---

## ✅ 总结

1. **优先尝试**：配置系统代理（方案 1）
2. **备用方案**：使用项目中的 Gemini API
3. **长期方案**：保持代理常开，配置开机自启

**配置系统代理后，Cursor 应该可以正常使用 AI 功能了！** 🎉

