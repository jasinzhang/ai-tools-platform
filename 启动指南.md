# 服务器启动和访问指南

## 🚀 启动服务器

### 方法1: 使用npm（推荐）
```bash
npm start
```

### 方法2: 直接使用node
```bash
node server.js
```

### 方法3: 开发模式（自动重启）
```bash
npm run dev
```

## 🌐 访问网站

启动服务器后，你应该看到：
```
🚀 Server running on http://localhost:3000
📱 Environment: development
```

然后在浏览器中访问：
- **首页**: http://localhost:3000
- **工具页面**: http://localhost:3000/tools/password-generator.html

## 🔍 排查问题

### 如果页面无法打开：

1. **检查服务器是否启动**
   ```bash
   # 检查端口是否被占用
   lsof -i :3000
   ```

2. **检查是否有错误**
   - 查看终端输出的错误信息
   - 检查 `server.js` 是否有语法错误

3. **检查文件是否存在**
   ```bash
   ls -la public/index.html
   ls -la public/tools/
   ```

4. **测试服务器响应**
   ```bash
   # 在终端运行
   curl http://localhost:3000
   
   # 应该看到HTML内容
   ```

5. **运行测试脚本**
   ```bash
   node test-server.js
   ```

### 常见问题

**问题1: 端口被占用**
```bash
# 解决方案：使用其他端口
# 编辑 .env 文件
PORT=3001
```

**问题2: 找不到模块**
```bash
# 解决方案：重新安装依赖
npm install
```

**问题3: 权限问题**
```bash
# 确保有读取文件的权限
chmod -R 755 public/
```

**问题4: 浏览器缓存**
- 按 `Cmd+Shift+R` (Mac) 或 `Ctrl+Shift+R` (Windows) 强制刷新
- 或在浏览器中打开开发者工具，禁用缓存

## 📱 测试所有工具页面

访问以下URL测试各个工具：
- http://localhost:3000/tools/tiktok-title.html
- http://localhost:3000/tools/instagram-caption.html
- http://localhost:3000/tools/youtube-title.html
- http://localhost:3000/tools/text-rewriter.html
- http://localhost:3000/tools/text-summarizer.html
- http://localhost:3000/tools/qr-generator.html
- http://localhost:3000/tools/product-name.html
- http://localhost:3000/tools/username-generator.html
- http://localhost:3000/tools/password-generator.html
- http://localhost:3000/tools/color-palette.html

## 🛑 停止服务器

在运行服务器的终端按 `Ctrl+C`

或在另一个终端：
```bash
# 找到进程ID
lsof -ti:3000

# 停止进程
kill $(lsof -ti:3000)
```

## 📞 如果仍然无法访问

请提供以下信息：
1. 启动服务器时的完整输出
2. 浏览器访问时的错误信息（如果有）
3. 运行 `node test-server.js` 的输出结果

